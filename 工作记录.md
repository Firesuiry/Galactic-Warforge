2026-03-26

- 继续晚期真机回放，优先复测 `oil_fractionation -> sulfuric_acid -> graphene` 的末端混线。
- 发现真实服里 `19,18` 终端混线仍会被 `water` 二次占满，`stone_ore` 只能短暂打穿，`depot_mk1(19,15)` 会停在 `8 sulfuric_acid` 左右不再上涨。
- 先补了两层根因修复：传送带单源混合栈优先补不同物料、建筑出货优先写入不重复目标带；新增相关回归并通过：`go test ./internal/gamecore -run 'TestConveyorSettlement|TestBuildingIO'`、`go test ./internal/gamecore`。
- 已同步 `docs/player/已知问题与回归.md`；当前下一步应继续修 `sulfuric_acid` 末端占位，再往 `graphene -> carbon_nanotube -> solar_sail` 推进。

2026-03-25

- 修复 `client-cli` 启动崩溃：为 `shared-client` 补 `package.json`，让共享模块按 ESM 加载。
- 收紧默认 SSE 订阅，去掉 `damage_applied` / `entity_updated` 这类中后期高频事件刷屏。
- 完成一轮中后期试玩，打通 `lightweight_structure -> build_dyson_* -> demolish_dyson`。
- 确认当前主要晚期阻塞：`launch_solar_sail` 仍因 `no solar sails in inventory` 无法走自然玩家闭环。
- 已同步 `docs/dev/客户端CLI.md`、`docs/player/已知问题与回归.md`。
- 修复太阳帆发射主线阻塞：`launch_solar_sail` 改为只接受 `em_rail_ejector`，并从发射器本地装载的 `solar_sail` 发射。
- 为 `em_rail_ejector` 补上 `solar_sail` 输入口与存储，新增装载/发射回归测试并通过。
- 已同步 `docs/archive/design/03-建筑系统.md`、`docs/archive/design/07-戴森球系统.md`、`docs/dev/服务端API.md`、`docs/dev/客户端CLI.md`、`docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 继续真实试玩后发现默认 SSE 仍会被 `production_alert` 刷屏；现已从默认订阅中移除，并同步 `docs/dev/客户端CLI.md`、`docs/player/已知问题与回归.md`。
- 继续把后期科技推进到 `ray_receiver`，实机发现其会在零戴森供能下白送产出；现已修复为只消费真实轨道供能，补回归测试并同步 `docs/dev/服务端API.md`、`docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 继续晚期 CLI 试玩时发现 `water_pump` / `oil_extractor` 缺少运行时采集定义，建成后不会产水/产油；现已补齐运行时、限制必须贴对应资源点建造，并补回归测试通过。
- 已用真实 CLI 回放验证：`water_pump` / `oil_extractor` 现在会把 `water` / `crude_oil` 写入本地存储，并经相邻传送带送入 `depot_mk1`。
- 已同步 `docs/archive/design/03-建筑系统.md`、`docs/dev/服务端API.md`、`docs/dev/客户端CLI.md`、`docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 继续回放 `sulfuric_acid` 链路时发现生产建筑会把配方输入误推进输出侧、且多输入会长期偏食单路；现已按配方产物过滤输出，并按缺料优先吸料，补回归测试与 CLI 回放验证，通过后已同步 `docs/player/已知问题与回归.md`。
- 继续真实 CLI 搭 `oil_fractionation -> sulfuric_acid` 时发现生产建筑会把配方输入吸满库存，卡住 `pending_outputs` / `pending_byproducts`；现已按配方输入做限流，补 `TestStoragePortInputCapsProductionRecipeInputs`、`TestBuildingIOProductionInputCapLeavesRoomForOutputsAndByproducts` 并通过，已同步 `docs/player/已知问题与回归.md`、`docs/player/玩法指南.md`。
- 继续晚期试玩时已在真实地图上打通 `oil_extractor -> oil_fractionation -> sulfuric_acid`，确认 `chemical_plant` 能持续吃到 `refined_oil` / `stone_ore` / `water` 并产出 `sulfuric_acid`。
- 继续用 `build_dyson_shell + build ray_receiver` 复测时发现接收站只读取太阳帆能量、忽略戴森球总产能；现已修复并补 `TestSettleRayReceiversConsumeDysonSphereEnergyUpToInputCap`，同步 `docs/archive/design/07-戴森球系统.md`、`docs/dev/服务端API.md`、`docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 继续真实 CLI 回放 `oil_fractionation -> sulfuric_acid` 时发现 `hydrogen` 副产会在当前单口物流模型里堵死主线；现已把 `oil_fractionation` 收敛为只产 `refined_oil`，补回归测试并同步 `docs/archive/design/04-物流与生产系统.md`、`docs/dev/服务端API.md`、`docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 继续晚期 CLI 试玩，已解锁 `ray_receiver` / `lightweight_structure` / `high_strength_material`，并在真实地形上重新打通 `oil_extractor(16,20) -> oil_fractionation(16,16) -> sulfuric_acid(18,17)`，确认酸库存可灌到 `20+`。
- 这轮没暴露新的服务端逻辑 bug，但确认 `17,18` / `17,19` 是水面，旧纸面布局不成立；已把“先看可建地、给生产建筑预留出货边”的提醒同步到 `docs/player/玩法指南.md`、`docs/player/已知问题与回归.md`。
- 新临时服继续回放 `oil_fractionation -> sulfuric_acid` 时复现 merge 带长期饿死后路原料；现已修复传送带合流分配，补 `TestConveyorSettlement`，并用真实 CLI 确认 `19,18` 混线稳定保留 `water + stone_ore`、`depot_mk1(19,15)` 持续进酸。
- 新临时服补做 `build_dyson_node/frame/shell -> build ray_receiver` live 回放：`ray_receiver(5,5)` 已能把 `critical_photon` 连续送进 `depot_mk1(5,7)`，确认真实服已计入戴森壳总产能。
- 已同步 `docs/player/已知问题与回归.md`，并收敛下一轮重点为 `graphene -> carbon_nanotube -> solar_sail` 真机闭环。
