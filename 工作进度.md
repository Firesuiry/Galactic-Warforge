
## 2026-09-17 CLI 公开命令全覆盖

- CLI 具名动词覆盖 shared-client 公开命令目录；文档主表补齐监测器/配送器/舰队控制等。
- `summary` 输出执行体 ID 与背包，便于观察手搓/装料结果。

## 2026-09-17 DSP行星全内容实现(目标会话)

- 目标: 将《戴森球计划》行星内可完成的全部生产内容、科技树玩法在当前游戏实现并测试。
- 已做八维审计(工作流 wf_3203be90-088), 产出缺口清单: docs/guide/DSP行星内容缺口实现计划.md (批次A~G, 约40项)。
- 已完成行星内生产闭环: 科技-配方门控按目录生效；补齐钢材/玻璃/金刚石/晶格硅/有机晶体/棱镜/电浆激发器/发动机/电磁涡轮/超级磁环/粒子容器/增产剂Mk.I-II/燃烧单元/炮弹组/钛化弹药等配方与物品；熔炉/制造台/精炼/矩阵站走同一 Tick 结算；研究消耗矩阵后解锁配方。恒星系/终局配方仍 pending。
- 关键路径: A0门控修复 → A1~A3配方 → C1~C3科技效果 → D1/D2机甲 → E1~E3防御。

## 2026-09-18 DSP 行星内对齐 W1-W3（目录/接线/客户端同步）

- W1 目录对齐：物品/资源/配方/科技/建筑成本对齐 DSP 行星内数据基线（commit ca4f5b2/55a169e 基线冻结）。
- W2 玩法接线：电力/采集/物流/研究站/机甲效果/战利品落地（commit 5e3cda4），新增公开命令 `set_recipe`（原地切换配方，省略 recipe_id 时研究站回研究模式）；煤矿可达性改为结构性承诺（027de8e）。
- R1/W3 前置：新增 `set_energy_exchanger_mode`（蓄电器 charge/discharge/standby 物品循环），服务端目录与 API 文档同步。
- W3 客户端与文档同步（本轮）：
  - shared-client：`types.ts` 补 `EnergyExchangerMode` / `EnergyExchangerModule` / `SetRecipePayload` / `SetEnergyExchangerModePayload` 及 CommandType 联合；`api.ts` 新增 `cmdSetRecipe` / `cmdSetEnergyExchangerMode`；`command-catalog.ts` 注册两个新命令并修复 `install_logistics_vehicle` 单引号条目导致的覆盖率漏检。
  - client-cli：新增 `set_recipe` / `set_energy_exchanger_mode` 具名命令（含 help），npm test 67/67 绿（含 public-coverage）。
  - `scripts/command_coverage.py --check` 通过：shared/CLI/agent 三面 59/59 全覆盖。
  - 文档：客户端CLI.md 补两条命令；服务端API.md 校对 set_energy_exchanger_mode 错误码表述；玩法指南补 set_recipe 用法、蓄电器充放、增产剂 Mk.I→III 链、垂直叠层、矿机覆盖脉数与全矩阵产线说明。
