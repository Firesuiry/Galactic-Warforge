# 已知问题与回归

> 注意：本页主要保存历次试玩与回归记录。随着 T100 收口，其中关于“终局高阶舰队线仍隐藏/没有公开入口”的旧条目已经过时；阅读现状请优先以 `docs/player/玩法指南.md`、`docs/dev/服务端API.md` 与 `docs/dev/客户端CLI.md` 为准。

## 2026-04-05 T100 收口复测

### 试玩环境

- 派生官方 midgame：
  - 端口：`18180`
  - 数据目录：`/tmp/siliconworld-t100-BXMlF7/data`
  - 基于 `config-midgame.yaml + map-midgame.yaml` 临时追加：
    - `prototype`
    - `precision_drone`
    - `corvette`
    - `destroyer`
    - 对应 prerequisite tech
    - 对应载荷测试物资
- 玩家：`p1 / key_player_1`
- 客户端入口：
  - `client-cli`

### 本轮追加结论

- 终局高阶舰队线已经从“隐藏未开放”收口为公开正向链路：
  - `transfer b-1 prototype 2`
  - `deploy_squad b-1 prototype --count 2 --planet planet-1-2`
  - `transfer b-1 corvette 1`
  - `commission_fleet b-1 corvette sys-1 --count 1 --fleet-id fleet-demo`
  - `fleet_assign fleet-demo wedge`
  - `fleet_status fleet-demo`
  - `system_runtime sys-1`
- authoritative runtime 已确认可观察：
  - `planet runtime` 中出现 `combat_squads[0].id = squad-1`
  - `system runtime` 中出现 `fleets[0].fleet_id = fleet-demo`
  - `fleet detail` 可看到 `weapon` / `shield` / `formation` / `last_attack_tick`
- 队列语义已确认：
  - CLI 先返回 `accepted`
  - 下一 tick 在审计里落成 `executed`
- 部署枢纽供电门禁已确认：
  - 基地 `b-1` 在未接电时会被 authoritative 拒绝：
    - `deployment hub is not operational: power_no_provider`
  - 接入 `wind_turbine + tesla_tower` 后，`/world/planets/planet-1-2/networks.power_coverage` 显示 `b-1.connected = true`，部署与组舰命令即可执行成功
- 当前仍保留的真实边界：
  - `produce` 只负责 `worker` / `soldier`
  - 高阶单位必须先做成载荷 item，再走 `deploy_squad` / `commission_fleet`
  - `fleet_attack` 当前只支持同一恒星系目标；官方 midgame 本轮没有现成敌军，因此这里只验证了命令执行与后续 runtime 状态迁移

## 2026-04-04 终局补充复测

### 试玩环境

- 默认新局：
  - 端口：`18141`
  - 数据目录：`/tmp/sw-playtest-default.pxQw4P/data`
- 官方 midgame：
  - 端口：`18142`
  - 数据目录：`/tmp/sw-playtest-midgame.KBITxj/data`
- 玩家：`p1 / key_player_1`
- 客户端入口：
  - `client-cli`

### 本轮追加结论

- 默认新局的真实开局链路仍然成立：
  - `matrix_lab` 仍可作为开局第一座研究站
  - 未装矩阵时，`start_research electromagnetism` 会返回 `missing electromagnetic_matrix in research labs`
  - 向研究站装入 `10` 个 `electromagnetic_matrix` 后，`electromagnetism` 可正常完成并解锁 `wind_turbine` / `tesla_tower` / `mining_machine`
- 官方 midgame 下，以下 DSP 中后期建筑与公开命令仍可真实走通：
  - 建筑：
    - `orbital_collector`
    - `vertical_launching_silo`
    - `em_rail_ejector`
    - `ray_receiver`
    - `jammer_tower`
    - `sr_plasma_turret`
    - `planetary_shield_generator`
    - `self_evolution_lab`
    - `energy_exchanger`
    - `pile_sorter`
    - `recomposing_assembler`
    - `advanced_mining_machine`
    - `artificial_star`
  - 命令：
    - `switch_active_planet`
    - `transfer`
    - `build_dyson_node`
    - `build_dyson_frame`
    - `build_dyson_shell`
    - `launch_solar_sail`
    - `launch_rocket`
    - `set_ray_receiver_mode`
- 终局高阶舰队线的当前版本边界已经改为公开可玩：
  - `prototype`
  - `precision_drone`
  - `corvette`
  - `destroyer`
  当前 authoritative 口径是：
  - `/catalog.techs` 公开这 4 项科技
  - `/catalog.units` 公开其 `production_mode` / `deploy_command` / `query_scopes`
  - 玩家通过 `transfer + deploy_squad/commission_fleet + fleet_status/system_runtime` 走完整链路
- `thermal_power_plant`、`mini_fusion_power_plant`、`artificial_star` 这三类燃料型发电建筑现在统一遵守同一条运行态规则：
  - 无可达燃料时，`runtime.state = no_power`
  - 无可达燃料时，`runtime.state_reason = no_fuel`
  - 装回燃料后的下一 tick 会恢复 `running`

### 本轮边界收口记录

- 终局高阶舰队线已收口为正向回归项：
  - 复核：
    - 查询 `/catalog`
    - 查询 `/catalog.units`
    - 执行 `transfer + deploy_squad + commission_fleet + fleet_assign + fleet_status + system_runtime`
  - 当前口径：
    - 4 项科技公开可见且可研究
    - `/catalog.units` 公开 `worker`、`soldier`、`prototype`、`precision_drone`、`corvette`、`destroyer`
    - `produce` 仍只处理 `worker` / `soldier`
    - 高阶单位通过载荷 item + 部署命令进入 runtime
- 燃料型发电建筑无燃料时的运行态已统一：
  - 复核：
    - 官方 midgame 下执行 `build 11 5 artificial_star`
    - 查询 `inspect planet-1-2 building <artificial_star_id>`
  - 当前口径：
    - `artificial_star`、`thermal_power_plant`、`mini_fusion_power_plant` 在无可达燃料时统一显示 `no_power/no_fuel`
    - 装回燃料后，下一 tick 会恢复 `running`

## 2026-04-04 深度试玩补充

### 2026-04-04 深夜追加复测

#### 试玩环境

- 默认新局：
  - 端口：`18131`
  - 数据目录：`/tmp/sw-playtest-default.uq1XUp/data`
- 官方 midgame：
  - 端口：`18132`
  - 数据目录：`/tmp/sw-playtest-midgame.O9gEV2/data`
- 玩家：`p1 / key_player_1`
- 客户端入口：
  - `client-cli`

#### 本轮追加结论

- 默认新局入口没有回退：
  - `matrix_lab` 仍可作为第一阶段研究站
  - `start_research electromagnetism` 仍然要求先向研究站装填真实矩阵
  - `electromagnetism` 完成后，`wind_turbine`、`tesla_tower`、`mining_machine` 可继续真实建造
- 官方 midgame 下，戴森球计划相关建筑、科技门禁和玩法主链仍然总体成立：
  - `switch_active_planet` 可正常切换 `planet-1-2 -> planet-1-1`
  - `orbital_collector` 在气态行星接电后进入 `running`，并实际累积：
    - `hydrogen = 324`
    - `deuterium = 81`
  - `advanced_mining_machine` 在资源点上可进入 `running`
  - `em_rail_ejector`、`vertical_launching_silo`、`ray_receiver`、`jammer_tower`、`sr_plasma_turret`、`planetary_shield_generator`、`self_evolution_lab`、`recomposing_assembler`、`pile_sorter` 都能在官方 midgame 真实建出
  - `build_dyson_node` / `build_dyson_frame` / `build_dyson_shell` 执行后，`em_rail_ejector` 与 `vertical_launching_silo` 可从 `under_power` 恢复到 `running`
  - `launch_solar_sail b-39 --count 4 --orbit-radius 1.2` 返回成功
  - `launch_rocket b-40 sys-1 --layer 0 --count 2` 返回成功，并发出 `rocket_launched`
  - `set_ray_receiver_mode b-41 photon` 仍会因缺少 `dirac_inversion` 被正确拒绝
- 因此，本轮没有再发现新的“戴森球计划相关建筑 / 科技树 / 主线玩法未实现”缺口。

#### 新发现问题

- 本轮未再复现太阳帆批量发射 `entity_id` 冲突：
  - 复核：
    - `transfer b-39 solar_sail 4`
    - `launch_solar_sail b-39 --count 4 --orbit-radius 1.2`
  - 当前口径：
    - 同一批次的 4 条 `entity_created` 会携带 4 个不同的 `entity_id`
    - 太阳帆已迁入 snapshot-backed 的 `space` runtime
    - replay / rollback digest 也已覆盖太阳帆计数、system 数和总能量

### 2026-04-04 晚间追加复测

#### 试玩环境

- 默认新局：
  - 端口：`18121`
  - 数据目录：`/tmp/sw-playtest-default.nw7ICZ/data`
- 官方 midgame：
  - 端口：`18122`
  - 数据目录：`/tmp/sw-playtest-midgame.pJcO03/data`
- Web 验证：
  - `client-web` 通过 `VITE_SW_PROXY_TARGET=http://127.0.0.1:18122` 启到 `http://127.0.0.1:4173`
- 玩家：`p1 / key_player_1`
- 客户端入口：
  - `client-cli`
  - `client-web`

#### 本轮追加结论

- 本轮没有再发现新的“戴森球计划相关建筑 / 科技树 / 主线玩法未实现”缺口：
  - `orbital_collector`
  - `vertical_launching_silo`
  - `em_rail_ejector`
  - `ray_receiver`
  - `jammer_tower`
  - `sr_plasma_turret`
  - `planetary_shield_generator`
  - `self_evolution_lab`
  - `recomposing_assembler`
  - `pile_sorter`
  - `advanced_mining_machine`
  都已在官方 midgame 实际建造并走过运行态验证。
- 戴森相关命令链路本轮也已继续确认：
  - `build_dyson_node`
  - `build_dyson_frame`
  - `build_dyson_shell`
  - `launch_solar_sail`
  - `launch_rocket`
  - `set_ray_receiver_mode`
  - `set_ray_receiver_mode ... photon` 会因缺少 `dirac_inversion` 被正确拒绝
- 浏览器侧最小验证正常：
  - Web 可登录在线服务端
  - 可打开总览页与 `planet-1-2`
  - 命令面板可成功发送建造命令并收到回显

#### 新发现问题

- 新问题：`/state/stats` 的生产统计会把没有实际产出的中后期建筑也计入 `total_output`
  - 复现样例：
    - `recomposing_assembler (b-51)` 没有 `recipe_id`
    - `self_evolution_lab (b-52)` 没有 `recipe_id`
    - `vertical_launching_silo (b-35)` 持续 `input_shortage = true`
  - 但 `GET /state/stats` 仍返回：
    - `production_stats.total_output = 6`
    - `by_building_type.recomposing_assembler = 2`
    - `by_building_type.self_evolution_lab = 3`
    - `by_building_type.vertical_launching_silo = 1`
  - 已立项：
    - `docs/process/task/T097_戴森深度试玩中生产统计误将空转建筑计入总产出.md`

### 试玩环境

- 默认新局：
  - 端口：`18111`
  - 数据目录：`/tmp/sw-playtest-default.R25kEn/data`
- 官方 midgame：
  - 端口：`18112`
  - 数据目录：`/tmp/sw-playtest-midgame.Qphsbf/data`
- 玩家：`p1 / key_player_1`
- 客户端入口：`client-cli`

### 本轮已确认可用

- 默认新局的科研门禁已经是“真实矩阵装填后才能开研”：
  - `start_research electromagnetism` 会在未装矩阵时直接返回 `missing electromagnetic_matrix in research labs`
  - `transfer b-28 electromagnetic_matrix 10` 后再次开研，可正常完成并解锁 `wind_turbine` / `tesla_tower` / `mining_machine`
- 官方 midgame 已确认可建：
  - `orbital_collector`
  - `vertical_launching_silo`
  - `em_rail_ejector`
  - `ray_receiver`
  - `jammer_tower`
  - `sr_plasma_turret`
  - `planetary_shield_generator`
  - `self_evolution_lab`
  - `recomposing_assembler`
  - `pile_sorter`
  - `advanced_mining_machine`
  - `energy_exchanger`
- 官方 midgame 已确认可走通：
  - `build_dyson_node` / `build_dyson_frame` / `build_dyson_shell`
  - `transfer`
  - `launch_solar_sail`
  - `launch_rocket`
  - `set_ray_receiver_mode`
- `orbital_collector` 与 `advanced_mining_machine` 在释放高耗电负载后都能进入 `running` 并累积库存，说明它们不再属于“没做出来”的范畴。

### 本轮已收口

- T095 已完成：
  - `ray_receiver` 切到 `power` 后，官方 midgame 真实回放会同步抬高 `summary.players[].resources.energy`、`stats.energy_stats.generation` 与 `/world/planets/{planet_id}/networks.power_networks[].supply`
  - 切模式前已经存在的 `critical_photon` 库存/缓冲会保留，但切模式后的新增长已经停止
  - 完成记录：`docs/process/finished_task/T095_戴森接收站power模式失效与缺电状态误判.md`
- 缺电原因口径已收口：
  - 当 `power_coverage.connected = true` 但网络 `shortage = true` 或当前 tick 分配结果为 `0` 时，`inspect` / `scene` / `building_state_changed` 会统一显示 `under_power`
  - 如果只是病因变化而状态仍是 `no_power`，SSE 也会继续发 `building_state_changed`，并带上 `prev_reason`

## 试玩环境

- 日期：2026-04-03
- 玩法入口：`client-cli` 实际发命令推进
- 服务端：
  - 临时 playtest 配置
  - 端口 `18093`
  - `100 tick/s`
  - 独立数据目录 `/tmp/siliconworld-playtest-dyson.Yd3agO/data`
- 玩家：`p1 / key_player_1`
- 参考文档：
  - `docs/player/玩法指南.md`
  - `docs/dev/客户端CLI.md`
  - `docs/dev/服务端API.md`
  - `server/map.yaml`
- 本轮关键命令摘要：
  - `summary`
  - `planet`
  - `scene planet-1-1 ...`
  - `galaxy`
  - `system sys-1`
  - `scan_galaxy`
  - `scan_system sys-1`
  - `start_research ...`
  - `build 6 0 wind_turbine`
  - `build 4 2 tesla_tower`
  - `build 5 1 mining_machine`
  - `build 8 2 em_rail_ejector`
  - `launch_solar_sail b-33`
  - `build 9 4 ray_receiver`
  - `build_dyson_node ...`
  - `build_dyson_frame ...`
  - `build_dyson_shell ...`
  - `inspect planet-1-1 building b-40`
  - `build 10 4 orbital_collector`
  - `build 10 5 vertical_launching_silo`
  - `launch_solar_sail b-42`
  - `build 11 5 vertical_launching_silo --recipe small_carrier_rocket`

## 已推进到的玩法阶段

- 已从开局推进到戴森中后期阶段，实际完成并验证：
  - 基础供电、采矿、研究推进
  - `solar_sail_orbit`
  - `ray_receiver`
  - `structure_matrix`
  - `interstellar_logistics`
  - `interstellar_power`
  - `gas_giants`
  - `vertical_launching`
  - `lightweight_structure`
- 当前已实际验证成功的入口：
  - `em_rail_ejector` 可以建造，`launch_solar_sail b-33` 会按当前实现检查本地已装载的 `solar_sail`
  - `build_dyson_node`
  - `build_dyson_frame`
  - `build_dyson_shell`
  - `ray_receiver` 在存在戴森结构后会产出 `critical_photon`
- 关键验证结果：
  - `build_dyson_node sys-1 0 10 20 --orbit-radius 1.2` 成功创建 `p1-node-l0-latp1000-lonp2000`
  - `build_dyson_node sys-1 0 -10 -20 --orbit-radius 1.2` 成功创建 `p1-node-l0-latm1000-lonm2000`
  - `build_dyson_frame sys-1 0 ...` 成功创建框架
  - `build_dyson_shell sys-1 0 -15 15 0.4` 成功创建壳面 `p1-shell-l0-latm1500-latp1500`
  - `inspect planet-1-1 building b-40` 显示 `ray_receiver` 处于 `running`，并已累计：
    - `critical_photon inventory = 100`
    - `input_buffer = 10`
    - `output_buffer = 10`

## 当时问题列表（含后续已修复项）

- 问题 1：多项戴森中后期科技已经研究完成，但实际没有解锁对应配方
  - 复现：
    - 完成本轮科技链后查询 `/catalog`
    - 观察 `high_strength_crystal`、`titanium_alloy`、`quantum_chip`
  - 实际现象：
    - `high_strength_crystal.unlocks = null`
    - `titanium_alloy.unlocks = null`
    - `quantum_chip.unlocks = null`
    - `lightweight_structure` 只剩 `dyson_component`，没有 `frame_material` 配方
    - `/catalog` 中不存在这些配方：`titanium_crystal`、`titanium_alloy`、`frame_material`、`quantum_chip`、`small_carrier_rocket`
  - 影响：
    - 科技树能点到后期，但不少 DSP 中后期物料链无法实际搭建
    - 戴森材料、量子芯片、火箭链路都断开
  - 临时绕过：
    - 无。当前只能继续使用不消耗材料的 `build_dyson_*` 系统入口验证结构结算

- 问题 2：`vertical_launching_silo` 只有建筑壳体，没有可玩的火箭闭环
  - 复现：
    - 研究 `vertical_launching`
    - `build 10 5 vertical_launching_silo`
    - `launch_solar_sail b-42`
    - `build 11 5 vertical_launching_silo --recipe small_carrier_rocket`
    - `inspect planet-1-1 building b-42`
  - 实际现象：
    - `launch_solar_sail b-42` 返回 `INVALID_TARGET: only EM Rail Ejector can launch solar sails`
    - `build ... --recipe small_carrier_rocket` 返回 `VALIDATION_FAILED: unknown recipe: small_carrier_rocket`
    - `b-42` 运行态里虽然有 `launch.rocket_item_id = small_carrier_rocket` 和 `production` 模块，但建筑只会长期 `idle/input_shortage`
  - 影响：
    - 垂直发射井无法生产火箭，也没有任何独立发射命令
    - 当前玩家只能“造出建筑”，不能进入火箭装填、发射、挂接戴森结构的实际玩法
  - 临时绕过：
    - 无

- 问题 3：默认一局无法实际进入气态行星 / 轨道采集器玩法
  - 复现：
    - 查询 `system sys-1`
    - 研究 `gas_giants`
    - `build 10 4 orbital_collector`
  - 实际现象：
    - 当前系统只有一颗 `rocky` 行星：`planet-1-1`
    - `build 10 4 orbital_collector` 返回 `INVALID_TARGET: orbital collector must be built on a gas giant`
    - `server/map.yaml` 当前还是 `system_count: 1` 且 `planets_per_system: 1`
  - 影响：
    - `gas_giants` 和 `orbital_collector` 虽然科技可解锁，但默认游玩路线里没有可落地对象
    - 星际 / 气态行星相关玩法在默认地图上不可达
  - 临时绕过：
    - 只能换自定义地图或自定义随机结果；默认 `map.yaml` 试玩无法覆盖

## 下一轮试玩重点

- 补齐缺失配方后，重跑：
  - `titanium_crystal`
  - `titanium_alloy`
  - `quantum_chip`
  - `frame_material`
  - `small_carrier_rocket`
- 在补齐垂直发射井命令后，验证：
  - 火箭生产
  - 火箭装填
  - 发射到戴森结构
- 在提供多行星 / 气态行星地图后，重跑：
  - `interstellar_logistics`
  - `gas_giants`
  - `orbital_collector`
