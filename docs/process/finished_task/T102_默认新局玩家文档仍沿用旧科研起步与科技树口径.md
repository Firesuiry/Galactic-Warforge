# T102 默认新局玩家文档仍沿用旧科研起步与科技树口径（已完成）

## 完成结论（2026-04-05）

- 默认新局的对外文档口径已统一回当前真实实现：
  - 开局只预完成 `dyson_sphere_program`
  - 该科技直接提供 `matrix_lab + wind_turbine`
  - `battlefield_analysis_base` 本身不发电
  - 第一段真实入口固定为“先风机、再研究站、再装矩阵、再开 `electromagnetism`”
  - `electromagnetism` 当前只解锁 `tesla_tower + mining_machine`
- 已同步修正文档：
  - `docs/player/玩法指南.md`
  - `docs/player/上手与验证.md`
  - `docs/player/已知问题与回归.md`
  - `docs/dev/服务端API.md`
- 已同步回收仍对外可见的历史摘要误导：
  - `docs/process/finished_task/T095_戴森接收站power模式失效与缺电状态误判.md`
  - `docs/process/finished_task/T099_戴森终局高阶舰队线未开放与人造恒星燃料态异常.md`
  - `docs/process/finished_task/T100_戴森深度试玩确认终局高阶舰队线仍未开放.md`

## 实现取真来源

- `server/internal/model/tech.go`
  - `dyson_sphere_program.unlocks = matrix_lab + wind_turbine`
  - `electromagnetism.unlocks = power_pylon + mining_machine`
  - 对外目录会把 `power_pylon` 归一化为 `tesla_tower`
- `config/defs/buildings/combat/battlefield_analysis_base.yaml`
  - `generation_mw = 0`
- `server/config-dev.yaml`
  - 默认启动包：`minerals = 240`、`energy = 100`、`electromagnetic_matrix x50`
- `server/internal/model/t092_default_newgame_test.go`
- `server/internal/gamecore/t092_default_newgame_test.go`
  - 两组 T092 回归测试共同锁定默认新局的早期科研闭环

## 文档收口范围

- 玩家主文档不再把基地描述成可直接给第一台研究站供电
- 不再把第一台 `wind_turbine` 继续写成 `electromagnetism` 的解锁项
- 验证文档中的最小命令链改为：

```text
build 3 2 wind_turbine
build 2 3 matrix_lab
start_research electromagnetism
transfer <matrix_lab_id> electromagnetic_matrix 10
start_research electromagnetism
```

- 历史摘要保留时间线，但把仍可能被摘录引用的默认新局结论改为当前实现口径

## 验证要求

- 文本检索：
  - 对 `docs/player`、`docs/dev` 与相关 `finished_task` 摘要执行“旧默认新局入口口径”关键词检索
  - 结果应只剩历史说明或完成记录，不应再在主文档里把旧入口当成现状

- T092 回归：

```bash
cd /home/firesuiry/develop/siliconWorld/server
env PATH=/home/firesuiry/sdk/go1.25.0/bin:$PATH go test ./internal/model ./internal/gamecore -run T092
```

- 真实默认新局重放：
  - 先建 `wind_turbine`
  - 再建 `matrix_lab`
  - 未装矩阵开研应返回缺矩阵
  - 装入 `10` 个 `electromagnetic_matrix` 后，`electromagnetism` 应正常完成
  - 研究完成后只解锁 `tesla_tower + mining_machine`
