# T060 液体与气体基础模型

## 需求细节
- 定义液体/气体的类型与状态描述结构（容量单位、密度或等效量级）。
- 定义管线段基础参数数据结构：流速、压力、容量、衰减系数（可空）。
- 定义管线段与节点的最小运行态数据：当前流量、缓存量、压力水平。
- 明确序列化与快照存储结构，确保可回放与审计。

## 实现记录
- 新增液体/气体基础模型结构（类型定义、状态结构、表单校验）。
- 新增管线节点/管线段参数与运行态结构，以及管线网络状态容器与深拷贝。
- 在世界状态与快照中纳入管线网络序列化/恢复逻辑，并补齐回放时的状态拷贝。
- 扩展快照回放测试覆盖管线网络数据。

## 测试记录
- /home/firesuiry/sdk/go1.25.0/bin/go test ./... (server)
