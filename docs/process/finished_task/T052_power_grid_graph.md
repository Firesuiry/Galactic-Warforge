# T052 电网图与连接规则

## 需求细节
- 电网拓扑图：定义电网节点/边的数据结构与索引方案。
- 连接规则：建筑连接、线路连接、跨区传输的建模与校验。
- 拆建更新：建筑/线路新增或移除时的电网图更新与一致性检查。

## 交付物
- 服务端电网拓扑图与连接规则实现。
- 关键规则测试覆盖（连接、断开、跨区传输）。
- 若涉及服务端 API 行为变更，更新 `docs/dev/服务端API.md`。

## 完成情况
- 新增电网拓扑图数据结构与空间索引，支持构建/增删/一致性校验。
- 实现线路与无线连接规则，补齐无线跨区传输范围配置。
- 补齐电网建筑连接点并在建筑建造/拆除与快照恢复时同步更新电网。
- 未涉及服务端 API 行为变更，无需更新 `docs/dev/服务端API.md`。

## 测试
- `cd server && /home/firesuiry/sdk/go1.25.0/bin/go test ./...`
