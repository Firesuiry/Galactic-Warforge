# T054 电网聚合与拆分合并

## 需求细节
- 电力流聚合：同一电网内供需聚合与结算模型。
- 拆分/合并：电网断开与重连的状态迁移与数据重建。
- 状态同步：电网状态变化触发的建筑供电状态更新。

## 交付物
- 服务端电网聚合、拆分与合并规则实现。
- 关键规则测试覆盖（聚合结算、拆分、合并、状态迁移）。
- 若涉及服务端 API 行为变更，更新 `docs/dev/服务端API.md`。

## 完成情况
- 新增电网聚合解析，按电网连通分量汇总供需与净值，并提供建筑到电网的映射。
- 聚合默认使用 `PowerInputs` 作为发电输入，供需拆分/合并时自动重建并保持分量内确定性排序。
- 供电状态同步沿用现有 Tick 结算对最新电网覆盖的更新机制，无额外 API 变更。
- 补充聚合总量、拆分与合并的模型测试覆盖。

## 测试
- `cd server && /home/firesuiry/sdk/go1.25.0/bin/go test ./...`
