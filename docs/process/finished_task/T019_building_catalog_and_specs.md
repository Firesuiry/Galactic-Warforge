# T019 建筑分类与规格定义

## 需求细节
- 建筑大类与子类枚举：采集、运输、仓储、生产、化工、精炼、电力、电网、研究、物流枢纽、太空与戴森球、指挥与信号。
- 统一建筑定义结构：唯一ID、名称、所属类别、占格尺寸、建造成本、解锁条件。
- 与 `docs/archive/reference/建筑功能说明.md` 对齐建筑清单与规格字段，补齐缺失项并消除重复。
- 产出初始数据与加载方式（内置常量或配置文件），保证可扩展。

## 前提任务
- T001 世界与地图
- T003 资源、物品与配方系统
- T014 服务端接口约束与命令驱动

## 实现结果
- 新增建筑分类/子类枚举与建筑规格结构（唯一ID、名称、类别、占格尺寸、建造成本、解锁条件、可建造标记），并提供建筑目录查询与覆盖加载能力（支持 YAML/JSON）。
- 按 `docs/archive/reference/建筑功能说明.md` 补齐建筑清单与规格默认数据，提供全量初始 catalog。
- 将核心建筑 ID 与流程对齐为 DSP 风格（采矿机、太阳能板、制造台等），并同步更新建造/生产逻辑与配方可用建筑。
- 更新服务端 API 文档中 `building_type` 取值。
- 增加建筑目录一致性测试。

## 测试
- /home/firesuiry/sdk/go1.25.0/bin/go test ./... (workdir=server)
