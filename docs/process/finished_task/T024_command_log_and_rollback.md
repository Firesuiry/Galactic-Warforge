# T024 命令日志与回滚

## 需求细节
- 命令日志：按 Tick 记录命令序列、来源与元数据。
- 回滚机制：基于快照回退到指定 Tick 并重放命令。
- 日志裁剪：与快照窗口联动，防止日志无限增长。
- 调试接口：提供回放/回滚的运维或测试入口。

## 前提任务
- T014 服务端接口约束与命令驱动
- T023 Tick 级状态快照与重放

## 完成情况
- 命令日志补全来源与元数据记录（issuer、enqueue_tick），并新增基于快照窗口的日志裁剪机制。
- 新增回滚机制：基于快照恢复后按 Tick 重放命令日志，回写世界与发现状态，并清理事件历史/命令日志/快照与队列。
- 新增调试接口 `POST /rollback`，返回回滚摘要与裁剪统计，并更新服务端 API 文档。
- 新增快照存储裁剪（TrimAfter）与事件历史裁剪能力，补充对应测试。

## 测试
- /home/firesuiry/sdk/go1.25.0/bin/go test ./... (workdir=server)
