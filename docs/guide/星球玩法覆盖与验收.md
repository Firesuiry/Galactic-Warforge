# 星球玩法覆盖与验收

目标：完成《戴森球计划》星球内可建设设施及其玩法，包括玩家机甲。目录条目、模型、可建造标记、单元测试均不能单独证明整项玩法完成。以下是 2026-09-16 当前代码核查，后续迭代继续按全量目标验收。

## 当前已确认的缺口

- `executor` 现有可战斗核心、燃料/电网补能、护盾、手动采矿和个人制造；`mecha` 仍是量产地面战斗单位。完整玩家机甲尚未完成：独立燃料仓管理、建造无人机、飞行、装备、死亡恢复仍待实现；采矿/制造/充电目前有真实 Tick 专项测试；默认新局采集、燃料补能、制造取消退款与建电网充电已通过浏览器操作，完整发展路线仍须验收。
- 已移除 `applyTechUnlocks` 空效果分支。核心容量、驱动引擎移动范围、能量护盾和研究速度已在实际结算点应用，其他科技效果仍须逐项核实，不能据目录声明认定生效。
- 自动集装机目前共用传送带缓存。`ConveyorState.MaxStack` 表示总物料容量，尚无独立货物层数/叠高吞吐模型，因此“真实集装增运”仍未完成。
- 截至本次核查，默认目录有 62 种建筑。每种均可建造，并不等于都有完整运行玩法。原先按名称合并为 17 项的缺口（制造台 Mk.II/Mk.III 分开计数）正在逐项清理；当前仍需继续补齐：四向分流器的立体端口布局、流速监测器、物流配送器、行星物流站、星际物流站、制造台 Mk.II、制造台 Mk.III、分馏塔、微型粒子对撞机、地热电站、信号塔，以及地基的批量铺设/土壤经济/资源掩埋规则。

## 2026-09-16 原 runtime 缺口逐项审计与进展

本表按“能否由公共建造/API 操作触发，并在 Tick 中产生该建筑应有的状态或资源变化”判断。`building_defs.go` 的目录条目、科技解锁、模型和只断言类型存在的测试不计作闭环证据。原缺口描述将 Mk.II/Mk.III 合并计算，实际逐项核对为 17 个建筑类型（制造台两种分别计数）。

| 建筑 | 现有可定位实现 | 当前结论 | 真实缺口与下一优先级 |
| --- | --- | --- | --- |
| `splitter` | [splitter.go](../../server/internal/model/splitter.go)、[conveyor_settlement.go](../../server/internal/gamecore/conveyor_settlement.go)、[splitter_commands.go](../../server/internal/gamecore/splitter_commands.go) | **部分完成（四口平面物流闭环已有）**。真实缓存24、进出各6/tick、零耗电；独立端口、输入/输出优先级、出口物品过滤、公平轮转和堵塞旁路均接统一运输结算。CLI建造/装料、Web配置、两仓分类收货和堵塞恢复已通过。 | 仍需立体端口/不同高度布局和普通新局科技推进验收；不能据单层四口测试认定所有DSP运输完成。 |
| `traffic_monitor` | [building_defs.go](../../server/internal/model/building_defs.go)；生产告警仅见 [production_monitor.go](../../server/internal/model/production_monitor.go) | **未完成**。没有按传送带流量采样、阈值、报警或控制输出的建筑实例状态；解锁和目录测试不能证明可用。 | 绑定相邻传送带，记录窗口吞吐/堵塞，产生可查询告警并验证 UI。 |
| `logistics_distributor` | [building_defs.go](../../server/internal/model/building_defs.go)；物流 Tick 入口是 [settlement_pipeline.go](../../server/internal/gamecore/settlement_pipeline.go) 的 station drone/ship 结算 | **未完成**。配送器没有专用状态，也未进入无人机调度候选；现有物流只识别 `IsLogisticsStationBuilding` 的站点。 | 实现配送器库存/范围/机器人绑定、供需调度及缺电缺货边界。 |
| `planetary_logistics_station` | [building_logistics_station.go](../../server/internal/model/building_logistics_station.go)、[logistics_dispatch.go](../../server/internal/gamecore/logistics_dispatch.go)、[logistics_drone_settlement.go](../../server/internal/gamecore/logistics_drone_settlement.go)；建造时在 [construction.go](../../server/internal/gamecore/construction.go) 注册 | **部分完成（物流闭环已有）**。站点配置、容量缓存、无人机派遣和交付有 Tick 测试；但没有 `defaultBuildingRuntimeDefinitions` 的端口/耗电/储存 runtime，物流不受该建筑电网运行态约束，仍不能算完整 DSP 建筑。 | 将站点容量、耗电、输入输出端口和停电行为接入 runtime/power，再做浏览器跨站实测。 |
| `interstellar_logistics_station` | 上述 station 逻辑；[logistics_ship_settlement.go](../../server/internal/gamecore/logistics_ship_settlement.go)；[logistics_station.go](../../server/internal/model/logistics_station.go) 的星际设置 | **部分完成（星际运输闭环已有）**。跨星球船、曲速配置和货物交付有测试；同样缺独立 runtime、电力、燃料/曲速消耗与站点工作态约束。 | 补齐站点 runtime 和航线能源/燃料边界，浏览器跨星球回放。 |
| `assembling_machine_mk2` | [building_runtime.go](../../server/internal/model/building_runtime.go)、[recipe.go](../../server/internal/model/recipe.go)、[production_settlement.go](../../server/internal/gamecore/production_settlement.go) | **部分完成**。已有独立生产、储存、端口和耗电 runtime；继承全部 Mk.I 配方及 prototype，真实产速 2 倍。断电、满仓、皮带进出及浏览器建造/产出已验证；普通新局科技材料推进仍待验收。 | 补全端到端建造与物流回放，验证相对 Mk.I 的吞吐/容量/功耗差异。 |
| `assembling_machine_mk3` | [building_runtime.go](../../server/internal/model/building_runtime.go)、[recipe.go](../../server/internal/model/recipe.go)、[production_settlement.go](../../server/internal/gamecore/production_settlement.go) | **部分完成**。已有独立生产、储存、端口和耗电 runtime；继承 Mk.I/Mk.II 配方及 precision_drone，真实产速 3 倍。断电、满仓、皮带进出及浏览器建造/产出已验证；普通新局科技材料推进仍待验收。 | 补全端到端建造与物流回放，验证相对 Mk.II 的吞吐/容量/功耗差异。 |
| `fractionator` | [fractionation.go](../../server/internal/model/fractionation.go)、[fractionation_settlement.go](../../server/internal/gamecore/fractionation_settlement.go)、[fractionation_test.go](../../server/internal/gamecore/fractionation_test.go) | **部分完成（真实分馏回流已实现）**。独立状态/6电/tick，西进氢、东回氢、南出氘，6次/tick、基础概率1%；喷涂消耗、两路背压、断电、外部皮带回路和随机状态存档均有专项测试。 | 固定端口尚不可旋转，不支持管道；普通新局科技/氢与增产剂原料可达、完整浏览器回放仍须验收。 |
| `miniature_particle_collider` | [building_runtime.go](../../server/internal/model/building_runtime.go)、[recipe.go](../../server/internal/model/recipe.go)、[particle_collider_test.go](../../server/internal/gamecore/particle_collider_test.go) | **部分完成（三配方加工已实现）**。生产/储存/皮带IO/24电/tick runtime已补齐；制氘、奇异物质、反物质与氢副产物均进入真实结算。实际建造、输入输出守恒、科技门槛、断电、满仓原子提交和存档恢复测试通过。 | 普通新局粒子容器/临界光子等全链可达与完整浏览器产线验收仍待完成；引力透镜仅有科技解锁字符串，物品与配方缺失，不能宣称完整后期加工链。 |
| `geothermal_power_station` | [building_defs.go](../../server/internal/model/building_defs.go)；[power_generation.go](../../server/internal/gamecore/power_generation.go) 的能源来源未按该类型结算 | **未完成**。无 runtime `Energy.OutputPerTick`，也没有地热地形/火山条件读取；建造只消耗材料，不发电。 | 增加地热点资格、发电 Tick、电网接入和地形枯竭/不可建边界。 |
| `missile_turret` | runtime `Combat` + `Storage`，`settleTurrets` | **部分完成（地面结算已实现）**：自动选取射程内敌方单位，消耗 `ammo_missile`，按冷却造成 `damage_applied`；缺弹停止并发状态事件。 |
| `implosion_cannon` | runtime `Combat` + `Storage`，`settleTurrets` | **部分完成（地面结算已实现）**：使用 `gravity_missile`，独立射程/伤害/冷却，缺弹停止。 |
| `laser_turret` | runtime `Combat` + `Energy`，`settleTurrets` | **部分完成（地面结算已实现）**：有电时自动锁定并按冷却造成伤害；供电系统进入 `no_power` 时停火。 |
| `plasma_turret` | runtime `Combat` + `Storage`，`settleTurrets` | **部分完成（地面结算已实现）**：使用 `plasma_capsule` 等离子胶囊（现有制造台配方可产），按独立射程/冷却自动攻击；缺弹停火并发状态事件，机甲护盾优先吸伤。 |
| `signal_tower` | [enemy_force_settlement.go](../../server/internal/gamecore/enemy_force_settlement.go) `applySignalTowerEffects`；[war_sensor_contacts.go](../../server/internal/gamecore/war_sensor_contacts.go) 生成传感器 contact | **部分完成（侦测/重定向有效）**。运行中信号塔可增加视野并按概率清除敌方目标；但没有 runtime 定义/耗电，效果只在敌对势力系统，缺 UI 局势联动与可验证的停电边界。 | 补独立传感器 runtime、电网约束、范围配置和浏览器局势显示。 |
| `foundation` | [planning.go](../../server/internal/model/planning.go)、[construction.go](../../server/internal/model/construction.go)、[gamecore/construction.go](../../server/internal/gamecore/construction.go) | **部分完成**。普通 `build` 搭配 `building_type=foundation` 可在水面/熔岩/阻挡地形排队，完成时将占用地形转为可建并记录原地形；拆除恢复原地形，材料不足不会改地形。水面铺设→同格风机→拆风机→拆地基恢复水面的真实 UI 回放及存档恢复已通过；仍缺批量铺设、土壤/资源掩埋规则。 | 实现 foundation 批量铺设、土壤经济和资源掩埋规则，补存档/浏览器验收。 |

因此，本节中炮塔已有地面攻击闭环，地基已有单格填平闭环，行星/星际物流站和信号塔具有部分专用 Tick 行为；其余条目仍应保持“未完成/部分完成”，不能因目录、科技、模型或静态测试存在而标记为完成。

## 全量验收范围

| 系统 | 必须验证的玩法 |
| --- | --- |
| 玩家机甲 | 移动与飞行、能源/燃料/充电、背包、手动采集和制造、建造无人机、装备战斗/护盾、科技升级、死亡恢复 |
| 采集 | 矿机/大型矿机覆盖与速度、抽水、油井衰减、矿脉消耗、稀有资源、轨道采集器的星球条件 |
| 运输 | 三级传送带及高度/转弯/交叉、分拣器等级/过滤/距离、四向分流优先级、集装与解叠、流速报警、增产剂喷涂 |
| 仓储物流 | 仓库/液罐、配送器和机器人、行星/星际站供需、无人机/运输船与能源、物流过滤和容量 |
| 加工 | 各级熔炉/制造台、精炼三配方、化工厂/量子化工厂、分馏回路、对撞机、黑雾高级设施与全配方链 |
| 能源 | 风光火电、地热、聚变、人造恒星、蓄电器充放电/能量枢纽、电网覆盖/优先级/欠压、射线接收器 |
| 科研与行星发射 | 六种矩阵、研究站堆叠与科技效果、弹射器/发射井供料、太阳帆/火箭与接收器联动 |
| 建造与地形 | 规划/批量建造/蓝图、拆除退款/升级、地基/土壤/海面填平、资源掩埋恢复、球面跨面与极区 |
| 防御 | 各炮塔弹药/射程/目标/供电、干扰与信号、战地基站补给修理回收、行星护盾、黑雾地面攻防与战利品 |
| 展示与持久化 | 各建筑可辨识模型及真实工作动画、可操作 UI、错误回显、存档/重放一致性与性能 |

每项均需：正常新局科技/原料可达 → 公共 API 与玩家操作可达 → Tick 真实结算 → 缺电/缺料/堵塞边界 → 存档/重放 → 浏览器实测。保持未验证项为待完成。

## 2026-09-16 精炼闭环

- 为 `oil_refinery` 补齐生产、6 能量/tick、电网连接、仓储和输入/主产物/副产物端口。
- 现有 `oil_fractionation` 改为精炼厂执行等离子精炼：2 原油 → 2 精炼油 + 1 氢。
- 新增 `xray_cracking`：1 精炼油 + 2 氢 → 3 氢 + 1 高能石墨。
- 新增 `reformed_refinement`：2 精炼油 + 1 氢 + 1 煤 → 3 精炼油。
- 三配方均为 60 tick/批，分别使用等离子精炼、X 射线裂解、重整精炼科技。
- 同时作为输入和输出的物料分离存放：原料保持在库存/输入缓存；完成后的产物进入输出缓存。必须经外部回流或玩家补料开启下一批，不能在完成前直接被输出口抽走。输出缓存满时整批等待，不部分发货或丢失副产物。
- 回归：`server/internal/gamecore/refinery_test.go`；浏览器回放：`scripts/playtest-refinery.mjs`，需隔离 war-map 测试服解锁三种精炼科技并提供脚本头部列出的原料。可设置 `SW_REFINERY_WEB` 与 `SW_REFINERY_EVIDENCE`。

本轮已实测：隔离服 `19486`，Web `4176`；通过浏览器生产规划选择配方并建造三台精炼厂、补建三台风机、API 装料后读取真实产出。结果分别为 2 油+1 氢、3 氢+1 石墨、3 油，无浏览器错误。证据 `/tmp/sw-refinery-review/browser-results.json` 和 `/tmp/sw-refinery-review/refinery-browser.png`（临时文件可通过回放脚本重新生成）。此证据覆盖配置了科技和原料的测试场景，不能替代普通新局全科技推进验收。


## 2026-09-16 玩家机甲核心闭环

- `Unit.mecha` 保存核心能量、燃料余能、护盾和受击时间；存档 clone 使用统一 `Unit.Clone()` 深复制，避免查询/快照与运行态共享指针。
- `refuel_mecha` 从玩家真实背包扣有效燃料；支持燃料热值公开于物品目录。过量热值留存，禁止在核心满或有剩余燃料时重复补充。
- 移动按可走路径格数耗能，攻击耗能；拒绝命令不改变位置或伤害。手动攻击和炮塔攻击均先结算个人护盾。
- 核心、引擎、护盾科技按实际已完成等级幂等计算；研究速度影响真实矩阵消耗速率。单纯科技改变派生属性也会发机甲状态事件，前端重新拉取真实场景。
- Web 的玩家机甲使用机甲模型，情境栏/详情显示能量和护盾；补能表单读取服务端有效燃料目录。CLI 支持同一补能命令。
- 服务端专项回归包括完整消耗 100 蓝矩阵后增加核心容量、10→11 的研究吞吐、库存与燃料守恒、能源不足拒绝、护盾恢复和存档隔离。浏览器回放脚本 `scripts/playtest-player-mecha.mjs` 使用隔离近距双玩家场景；普通新局全流程验收仍未完成。

最终浏览器实测：隔离服 `19487`、Web `4177`，玩家机甲攻击后核心 101→93；煤提供 25 能量，其中 17 填充至 110、8 留作燃料余能；敌方反击使护盾 20→8、生命保持 120；随后地图移动至 `(4,2)` 并通过建造栏建成 `(4,4)` 风机。页面无 page/console error。证据 `/tmp/sw-mecha-review/browser-results.json`、`mecha-core.png`、`mecha-shield-hit.png`。测试场景预置科技和煤，不代表普通新局燃料获取全链已验证。服务端全量、CLI 41 项、前端相关 37 项与生产构建通过。


## 2026-09-16 制造台、地基与炮塔收尾

- 高级制造台补齐配方继承、建造成本及真实产速。统一 Production.Throughput 参与周期计算，亦作用于已有高级炉和化工厂，输入/产物数量保持守恒。新增断电、输出满仓恢复、皮带进出及速度边界测试。
- 制造台浏览器证据：`/tmp/sw-assemblers-review/first-build-results.json` 和 `browser-results.json`，三个等级各投入 2 铁锭、产出 2 齿轮，无页面错误；回放 `scripts/playtest-assemblers-browser.mjs`。
- 地基修复水面前端预检、同格工厂选中、重复铺设和拆除/施工互斥，补齐原地形存档；三种地图查询现在反映真实动态地形。地基模型使用薄铺装。
- 地基浏览器证据：`/tmp/sw-foundation-review/browser-results.json`，UI 机甲移动、`(0,9)` 水面铺地基、同格风机运行、依次拆除后恢复 water 全通过，无页面错误；回放 `scripts/playtest-foundation-browser.mjs`。
- 炮塔消费输入/库存/输出缓冲区中的真实弹药，修复存储自动移位后无法开火；按稳定实体顺序选目标，冷却期间不扣弹，无目标/友军/超距/缺电不射击。各已加载星球都进行炮塔结算；射击者也收到伤害事件。
- 等离子塔使用新可生产 plasma_capsule；高斯与磁化等离子塔仍需真实弹药消耗、聚爆炮的弹药种类/范围伤害，以及各防御设施完整目标类型/普通新局链路，不能据本轮基础结算宣称全防御完成。

本轮回放均为隔离场景预置科技/建造材料。普通新局全进度及文首列出的机甲、物流、加工、地热、土壤等缺口仍未完成。

最终炮塔浏览器证据：`/tmp/sw-defense-fix/browser-results.json`、`turret-browser.png`，装入 1 发导弹后弹药归零，敌方机甲护盾吸收 20、HP 120 → 108；射击玩家收到了 damage_applied，页面无错误。回放 `scripts/playtest-defense-browser.mjs`，需脚本头部说明的双玩家近距隔离场景。最终服务端全量、CLI 41 项、前端相关 39 项及生产构建通过；补充验证取消后的建造任务也不能绕过重复地基/拆除地基限制。


## 2026-09-16 玩家采矿、个人制造与电网充电

- `mine_resource` 在 2 格球面距离内逐件采集有限固体矿点，10 tick/件、1 核心能量/tick，扣真实矿点余量并入玩家背包；超范围/缺能暂停，矿点耗尽结束。通过此路径得到的煤可被 `refuel_mecha` 使用。
- `craft_item` 仅接收目录 `handcraft_allowed=true` 且科技可用的固体配方。预留所有批次原料，逐批按 duration 完成、1 核心能量/tick；`cancel_mecha_job`/死亡只返还未完成批次原料，不退已耗能。`MechaJob` 及预留数组在 clone/存档中深复制，恢复不重复扣料。当前为八种基础配方，不能据此认定个人制造全配方链完成。
- 无线输电塔在 6 格内最高 10/tick 给自有机甲补能、自耗 1/tick；电力感应塔在 4 格内最高 2/tick。使用同网真实余电，先满足建筑与蓄电；不同电网、缺电、暂停、超范围、死亡、满核心均不充。统一记录蓄电与机甲用电，修复蓄电充入后余电仍重复计入玩家余额的问题；玩家资源封顶不会导致再次扣能。
- 服务端证据：`server/internal/gamecore/mecha_jobs_test.go`、`mecha_charging_test.go` 和 `server/internal/query/catalog_handcraft_test.go`。已覆盖科技/原料边界、暂停恢复、取消/死亡退款、矿点争抢、存档隔离、独立电网、单塔共享速率、球面范围、燃料与电网不超充和蓄电不双花。
- CLI 三命令已加入公共注册、帮助和共享 API；全量 53 项测试通过（含真实服务端 war 场景回归）。新增 12 项测试验证实际请求 JSON 区分矿点 ID/配方 ID/取消载荷，拒绝不合法数量，并防止 `cancel_mecha_job --help` 被误作为执行命令。
- 默认 `config.yaml` / `map.yaml` 新局专项测试通过，无预置煤：真实 move → mine_resource → 10 tick → refuel_mecha，煤矿 `planet-1-1-res-44530` 位于 `(399,404)`，余量 151→150，背包采得 1 煤后用于补能，核心 79→100；余热继续留存。此证据是默认种子的服务端命令链，不等于任意种子/普通新局全科技流程或浏览器回放。

普通新局浏览器证据：`scripts/playtest-mecha-start-browser.mjs`，使用默认 `config-dev.yaml + map.yaml`、816 面长、空背包和唯一初始科技，未注入库存/地形/额外科技。真实 UI 采铁 3、煤 1，煤补能 49→74；制造第一批完成后取消，保留 1 铁锭并返还剩余原料，剩余铁矿 2；核心耗尽后通过 UI 建风机与电力感应塔，0→100 充电（采到 18 条 grid_charge=2 事件），再次制造，最终 2 铁锭 + 1 铁矿。浏览器无页面或控制台错误。

隔离环境端口 19493/4183，证据 `/tmp/sw-mecha-start-review/browser-results.json`、`final-ui-results.json`、`mecha-final-inventory.png`。软件渲染的均衡画质使自动化点击超时，改流畅画质后从同一存档继续，未重置库存；最终另开页面核对核心100和背包铁矿1的界面显示。脚本保留显式恢复阶段，完整单次无人干预回放尚待复跑，不能把分段完成写成单次通过。已修复空背包被误显示为持续读取中的界面问题。

上述证明起步机甲链路，不等于普通新局全科技推进。飞行、建造无人机、装备、独立燃料仓和死亡恢复仍是明确缺口。

## 2026-09-16 四向分流器真实运输

- 新增公开 `configure_splitter`，完整替换四口配置；至少一个输入与一个输出、同口不重叠；优先级只能选对应端口，过滤只能绑定输出口的有效物品。非法配置不改变原状态，配置更新保留缓存和累计搬运，重置轮转游标。CLI/API/Web均已接入。
- 与传送带共用一次 Tick 计划/提交结算：库存只预留一次，当 tick 收到的物品下一 tick 才能发出，真实喷涂元数据随货物转移。输入、输出分别共享6/tick限额，满仓背压；优先端口不可用时轮转其它合格口。无过滤出口可收任意物品，滤口只允许所选物品，旁路不能绕过过滤。
- 分流器不耗电，暂停/施工时停止收发；只有同属玩家、端口相容的皮带类设备可连接。机器和分拣器需经皮带接入，不能绕过分流端口直接访问缓存。新增低矮四口模型，皮带按真实端口连续连接；封闭缓存不画未经服务端证明方向的货物流。
- 结算/命令/真实建造、24个跨面接缝、100 tick喷涂混料回环守恒、输入背压公平、快照/JSON/查询深复制均有专项测试。缺少真实分流器状态的旧存档明确拒绝恢复，不会把旧装饰性实体伪装为可运行新分流器。

浏览器回放 `scripts/playtest-splitter-browser.mjs`：隔离端口19495/4185、48面长全平地，预置war场景科技和物资、没有预置产线，CLI从零建造并真实transfer装料；因此该证据不代表普通新局科技解锁全链。UI配置东口只过铁、南口只过铜后，分别收到12铁/12铜且串料0；拆除东仓使两节东带各装满2，另20铁中4留东带、16由南口旁路；重建东仓清空4件积压，再加12铁全部走东优先，累计实际输出56件。页面与控制台错误均为空。

证据 `/tmp/sw-splitter-review/browser-results.json`、`commands.jsonl`、`splitter-filtered.png`、`splitter-blocked-bypass.png`、`splitter-priority-restored.png`、`splitter-line-overview.png`。

最终服务端重编并从原存档恢复后，又装入12铁：东仓16→28、南仓保持16，玩家+仓库+带内铁总量108→108，累计输出56→68。证据 `final-build-conservation.json`。本轮服务端全量 `go test ./...`、CLI全量57项、前端机甲/分流/模型/运输相关53项及生产构建通过。

小地图首屏修复：未知图集尺寸时仅请求可被服务端裁剪的矩形窗口；得到真实尺寸后再裁剪窗口和球面邻域中心，避免face16场景请求near=(48,32)越界400。页面与查询边界15项测试通过。

小地图真实浏览器补验亦通过：首次160×160矩形请求不带near且HTTP200；后续窗口裁剪48×32、near=(24,16)返回200并显示3D，页面/控制台无错误，证据 `small-map-browser.json`、`small-map-loaded.png`。本轮自启隔离服务均已停止。

## 2026-09-16 分馏、喷涂与对撞机

- `fractionator` 已补专属 runtime 和 `building.fractionation`，不伪装成普通库存配方。输入氢/失败氢分别保存真实 `ItemStack`，氘单独计数；三缓存各24，处理6/tick、耗电6/tick。西进氢、东回氢、南出氘固定；失败的氢必须经外部回流再次进入。基础成功率1%，喷涂Mk.I/II/III时1.25%/1.5%/2%，每次尝试扣该件氢1次喷涂余量，失败保留剩余效果。任一路输出缓存满、断电或暂停不继续尝试，不重抽随机数。
- `spray_coater` 已接真实西进东出货物、北侧增产剂，处理6/tick、耗电2/tick；`building.spray_coater` 保存输入/输出物品堆（各24）、累计喷涂/消耗、最近tick和已消耗剂量的 `spray_item_id` / `spray_units` / `spray_effect`。药剂使用普通Storage48、3槽；Mk.I/II/III每份提供12/24/60喷涂单位，每件货物获得4/6/8次效果。未用剂量持久保留，仍有喷涂余量的回流货物不重复耗剂，耗尽才重喷；缺剂货物原样通过。`transfer_item` 只许往喷涂机补有效增产剂，货物走西侧皮带。两设备都明确排除管道直连，不允许分流器缓存绕过其端口规则。
- 分馏 `attempts=converted+returned_hydrogen`，`rng_state` 与真实缓存、喷涂余量、累计数一起深复制和存档；恢复保留随机序列。缺少或损坏新专属状态的旧分馏塔/喷涂机快照会被明确拒绝。覆盖证据为 model/gamecore/snapshot 的分馏、喷涂专项测试；最终浏览器证据另行登记。
- 对撞机库存96/6槽、缓存24（输入16/输出8）、耗电24/tick、吞吐1，输入/主输出/副产口各6。三配方分别为：`deuterium_collision`，10氢→5氘，300tick，`miniature_collider`；`strange_matter`，2粒子容器+10氘+2铁锭→1奇异物质，480tick，`strange_matter`；`antimatter`，2临界光子→2反物质+2氢，120tick，`dirac_inversion`。已移除对撞机科技提前解锁反物质的漏洞。满仓整批保留主/副产物，不吞下一批原料；断电不推进，存档恢复不重复产出。
- 对撞机专项 [particle_collider_test.go](../../server/internal/gamecore/particle_collider_test.go) 验证全部配方皮带物料守恒、真实电网停机恢复、科技门槛、三建筑实际施工后连接输入/输出带，以及满仓原子提交和SaveFile JSON恢复。此轮 `go test ./internal/model ./internal/gamecore -count=1` 已通过；不以此代替普通新局科技和原料链验收。

本轮结论为**部分完成**：分馏/喷涂固定端口旋转、普通新局原料与科技全链、喷涂对通用制造台的完整效果仍未完成或未验收；引力透镜物品/配方链仍缺失。建筑模型和可查询状态不能替代完整玩法验证，也不代表全量目标完成。

已发现的电力边界：真实浏览器中，对撞机需求24、获配20时仍按完整速度推进；通用 `settleProduction` 当前仅检查 running，尚未按供电比例缩放生产进度。零供电会进入 no_power 并暂停。分馏/喷涂则已按分配比例缩放吞吐。部分供电语义统一仍待修复，不能将“断电暂停”解释为任意欠压都停机。

高级加工浏览器验收：隔离服19496/Web4186，CLI从零建造及装料，UI生产规划建成奇异物质对撞机。100份氢在真实外回路中最终为80氢+20重氢；未喷涂时概率1%，真实消耗1份Mk.III增产剂后概率2%，累计喷涂60件。对撞三配方分别产5重氢、1奇异物质、2反物质+2氢。拆除全部风机后连续12tick保持剩余113tick不变，恢复供电继续；预装118氢造成满仓时整批主/副产物等待，接输出带及仓库后最终收120氢和2反物质，无重复吞料。页面/控制台错误为空。证据 `test-results/advanced-processing/browser-results.json`，可用 `scripts/playtest-advanced-processing-browser.mjs` 重放。此场景预置科技、平地和材料，不代表普通新局全链完成。

最终服务端全量 `go test ./...`、CLI 57项、前端相关34项及生产构建通过。前端为三种设备增加独立模型与分馏/喷涂运行面板，连续带仅接真实有效端口（包含专属输入口的auto带）。本轮未新增三种设备的工作转子动画。

最终二进制补验：08:20:55 UTC重编（晚于全部服务端Go源码），从原存档恢复，恢复前/后及再次运行的氢+重氢均为100。浏览器实际展开“喷涂运行状态”“分馏运行状态”，累计值与固定端口方向文案可见；追加1份Mk.III后累计喷涂60→105，无页面/控制台错误。证据 `final-ui-recovery.json`、`spray-coater-status.png`、`fractionation-status.png`；本轮自启19496/4186服务已停止。
