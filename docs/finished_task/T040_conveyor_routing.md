# T040 传送带连接与路由规则

## 需求细节
- 输入/输出口规则：端口合法性校验与连接关系。
- 转向规则：直行、拐弯与方向继承的判定。
- 合流/分流：多输入合流的公平/优先策略，多输出分流的路由策略。
- 与基础网络的交互：连接变更对网络更新顺序与状态一致性的影响。

## 交付物
- 服务端传送带连接与路由规则实现。
- 若涉及服务端 API 行为变更，更新 `docs/服务端API.md`。

## 完成情况
- 传送带连接逻辑改为按端口规则计算可接入方向，支持直行、拐弯与多输入合流。
- 增加 `auto` 方向支持，用于多输出分流的路由优先级与自动继承方向。
- 连接更新按稳定排序与离线分配执行，确保结算顺序与结果一致性。
- 更新 `docs/服务端API.md` 的方向说明。

## 测试
- `cd server && /home/firesuiry/sdk/go1.25.0/bin/go test ./...`
