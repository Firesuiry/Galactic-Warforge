# T018 迷雾与可见性

## 需求细节
- 迷雾与可见性系统：按玩家视野裁剪地图与单位信息。
- 支持增量更新与缓存，避免全量计算。
- 可序列化玩家已探索区域。

## 前提任务

## 实现结果
- 可见性引擎改为带缓存的增量更新：按玩家维护视野来源、覆盖计数与可见/已探索网格，按 tick 仅同步变化来源。
- 行星迷雾接口扩展返回 `explored` 网格，非当前 active 行星返回可序列化的已探索缓存。
- 建筑/单位可见性裁剪改为基于缓存网格判定，避免每次查询全量计算。
- 更新服务端 API 文档说明迷雾接口新增字段。

## 测试
- /home/firesuiry/sdk/go1.25.0/bin/go test ./... (workdir=server)
