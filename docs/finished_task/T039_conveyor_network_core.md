# T039 传送带网络基础与流动规则

## 需求细节
- 传送带占格与方向建模：支持单格方向、可组合路径。
- 吞吐与堆叠：支持每 tick 的容量、堆叠上限与合规推进。
- 堵塞与溢出：当下游不可用时的停滞、回退与溢出处理策略。
- 基础运行循环：传送带网络的更新顺序与状态变更规则。

## 交付物
- 服务端传送带网络基础数据结构与流动核心逻辑实现。
- 若涉及服务端 API 行为变更，更新 `docs/服务端API.md`。

## 完成情况
- 新增传送带方向与缓冲模型，支持吞吐、堆叠上限与容量校验。
- 实现传送带网络结算，按 tick 推进、下游容量阻塞与回退。
- 构建/快照流程接入传送带状态，并支持 build 指令设置方向。
- 更新 `docs/服务端API.md` 的建筑字段与 build 参数说明。

## 测试
- `cd server && go test ./...`
