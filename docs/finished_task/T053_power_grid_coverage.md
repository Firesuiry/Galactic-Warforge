# T053 供电覆盖与接入规则

## 需求细节
- 覆盖判定：消费建筑的供电覆盖范围与接入规则。
- 接入约束：连接点容量、距离/邻接规则与跨区接入策略。
- 状态标记：无法接入时的状态标记与原因记录。

## 交付物
- 服务端供电覆盖与接入规则实现。
- 关键规则测试覆盖（覆盖判定、接入失败、跨区接入）。
- 若涉及服务端 API 行为变更，更新 `docs/服务端API.md`。

## 完成情况
- 新增供电覆盖解析与失败原因标记，按电网连通分量与连接点容量评估供电接入。
- 消费建筑默认补充隐式电力连接点，电网图纳入能耗建筑以支持覆盖判定与跨区（无线）接入。
- 结算阶段在能量扣除前应用供电覆盖检查，无法接入时标记 `no_power` 并记录原因。
- 更新 `docs/服务端API.md` 追加供电接入失败原因说明。
- 补充覆盖判定/接入失败/无线接入/容量耗尽的模型测试。

## 测试
- `cd server && /home/firesuiry/sdk/go1.25.0/bin/go test ./...`
